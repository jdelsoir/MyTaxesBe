# OS
.DS_Store

# Node
node_modules/

# Generated Pages site (rebuilt in CI from web/standalone.html + PWA assets)
web/dist/

# Local Claude config (not for publishing)
.claude/

# Large downloaded raw data (pipeline/raw also has its own .gitignore)
pipeline/raw/
